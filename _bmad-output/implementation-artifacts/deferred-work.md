## Deferred from: code review of story 5-3-context-enhanced-test-generation-prompts (2026-06-16)

- **[MEDIUM] Untrusted KB fields interpolated verbatim into the LLM prompt (prompt-injection surface)** — `buildKbContextBlock` emits module `name`/`description`/`dependencies`, RD `title`/`content`, endpoint `path`/`method`, flow `route`/`name`, `architecture_type`/`architecture_summary`/`tech_stack` raw into the test-generation prompt. The KB is produced by analyzing an external (attacker-controllable) application, so these strings can carry prompt-injection / instruction-override payloads. Systemic to all prompt builders in the codebase (not introduced by story 5.3); the spec designs for feeding this text to the LLM. Tracked as a separate security-hardening story — consider a shared `sanitizeForPrompt()` helper applied at all prompt-construction sites.
- **[LOW] `truncateContext` 1-byte overflow of the AC8 `maxChars + len(marker)` bound** — when a `\n\n` boundary lands at exactly index `maxChars`, `text.slice(0, boundaryIndex + 1)` keeps the first `\n` and the result length is `maxChars + 1 + len(marker)`. Impl is internally consistent and the tests use the `+1` bound. Two valid fixes (slice at `boundaryIndex` dropping both `\n`s, or amend AC8 to `maxChars + len(marker) + 1`). Benign; revisit if a strict downstream consumer enforces the literal bound. Location: `convex/ai/agents.ts:514`.
- **[LOW/Info] IDOR surface on `readKnowledgeBaseQuery` / `readBaselineRdQuery`** — both logic functions (`convex/ai/tools/logic.ts:39-69`, `:96-119`) query purely by `project_id` index with zero workspace-membership assertion; story 5.3's new `ctx.runQuery` calls inherit this trust chain. Calls are `internalAction`→`internalQuery` (not public-facing), so not a new B3 public-endpoint violation, but the 4-epic carry-forward D5/B3 surface is real. Already tracked under B3/D5; retained here for cross-reference.

## Epic 3 Triage (2026-06-14, from Epic 2 retrospective) — ALL RESOLVED

All items promoted for Epic 3 have been resolved during Epic 3 implementation or the post-Epic 3 retro. Retained for audit trail.

- **✅ RESOLVED (Story 3.2 AC5)** `searchProjectRag` rate limiting — `ragSearchPerWorkspace` wired (20/min/workspace via `@convex-dev/rate-limiter`).
- **✅ RESOLVED (Story 3.2 AC6)** `_getProjectWorkspaceForSearch` `.first()` ordering — pre-satisfied in code (`.order("desc").first()`); test added.
- **✅ RESOLVED (Story 3.2 AC7)** IDOR / cross-project scoping — `getOptionalOwnedEntity` already enforces membership; stale deferred-work comment de-staled.
- **✅ RESOLVED (post-Epic 3 retro, C5)** No `*-free` model guard — enforced in `getWorkspaceModel` (`convex/ai/model.ts`); all agent factories inherit automatically.

Items accepted at current scale (no action for Epic 3): the O(n²) read-amplification items, the `.first()` ordering on non-search paths, the test-quality gaps.



- useErrorLogger mock returns a fresh vi.fn() per call — logError invocations cannot be asserted [src/app/(auth)/projects/[id]/baseline/ExportBaselineRd.test.tsx, drift/ExportDriftReport.test.tsx] — The `useErrorLogger` mock factory creates a new `vi.fn()` on every render, so no test can assert the catch-block `logError` was called on failure. Hoist the mock via `vi.hoisted` and reuse a single fn instance. Test-quality only; no production bug.
- groupByDimension return order is implicit — drift markdown output order relies on helper's insertion order with no explicit sort [src/app/(auth)/projects/[id]/baseline/exportFormatters.ts — buildDriftReportMarkdown] — `buildDriftReportMarkdown` iterates `groupByDimension(report.items)` and emits `## {dimension}` sections in whatever order the helper returns. The test asserts `## Old RD vs Code` before `## Architecture Decision Drift`, but nothing in the formatter sorts groups by a canonical dimension order. If `groupByDimension` is refactored (e.g. to Map iteration), the test breaks for the wrong reason. Robustness concern, not a current bug.
- ExportBaselineRd test mocks useQuery to always return null — BMAD PRD path never tested with realistic non-empty ADRs [src/app/(auth)/projects/[id]/baseline/ExportBaselineRd.test.tsx] — The component test's `vi.mock("convex/react", () => ({ useQuery: vi.fn(() => null) }))` means `bmadMetadata` is always `null`, so the BMAD export only ever exercises the `adrs ?? []` empty path. ADR-content propagation into `buildBmadDecisionLogMarkdown` is untested at the component level (covered by formatter unit tests instead). Test-coverage gap.

## Deferred from: code review of 1-4-vector-embeddings-rag-storage (2026-06-13, initial)

- Stale embeddings after re-ingestion — no namespace cleanup [convex/knowledge/embeddingActions.ts] — RAG vector store retains embeddings for deleted files when repo is re-ingested. RAG's `delete`/`deleteByKey` methods exist but are never called. Should be addressed in story 1-8 (KB re-sync) which owns namespace lifecycle.
- searchProjectRag implemented as action not query [convex/knowledge/queries.ts:120] — rag.search() requires action context (CtxWith<"runAction">), so cannot be a query. Documented deviation from spec Task 5 which said "query". Future frontend consumers must use useAction() instead of useQuery(), losing real-time reactivity.
- No AbortController/network timeout on embedding calls [convex/knowledge/embeddingActions.ts] — Spec error handling table mentions "30s AbortController timeout" but RAG component manages fetch internally. Cannot easily inject timeout without forking the component. Relies on Convex's function-level timeout (~5 min for actions).
- Re-embeds entire chunk set on workflow retry [convex/knowledge/embeddingActions.ts] — Key-based upsert ensures correctness (no duplicates), but workflow retry after partial success re-embeds already-completed batches. Cost/latency impact only, no data integrity issue.

## Deferred from: code review of 1-4-vector-embeddings-rag-storage (2026-06-13, re-review)

- **[✅ RESOLVED — Story 3.2 AC5]** searchProjectRag rate limiting — `ragSearchPerWorkspace` wired (20/min/workspace via `@convex-dev/rate-limiter`). Cost abuse vector closed.
- **[✅ RESOLVED — Story 3.2 AC6]** _getProjectWorkspaceForSearch .first() ordering — pre-satisfied in code (`.order("desc").first()`); test added in Story 3.2.
- Sequential embedding, not batched — EMBEDDING_BATCH_SIZE only controls progress [convex/knowledge/embeddingActions.ts] — Each rag.add() is awaited sequentially. The RAG component likely supports batch insertion. For 10k chunks this is 10k sequential awaits. Performance concern only, not correctness.
- Separate mutations for ready+synced — partial failure [convex/knowledge/ingestionWorkflow.ts] — "ready" status and last_synced_at are in two separate step.runMutation calls. If first succeeds but second fails, workflow retries and re-runs embedChunks from scratch. Cost concern; key-based upsert ensures correctness.
- 429 ignores Retry-After header [convex/knowledge/embeddingActions.ts:78] — Uses fixed 30s instead of server-provided backoff. Requires extracting Retry-After from AI SDK error responseHeaders. Blocked by the 429 statusCode property fix (patch finding #2).

## Deferred from: code review of 1-5-ai-architecture-module-extraction (2026-06-13)

- Non-idempotent extraction on workflow retry [convex/knowledge/extractionActions.ts] — Phase 1 architecture patch is idempotent (overwrite). Module duplicates can't occur from normal operation (transactional mutation ensures all-or-nothing). Spec explicitly defers _deleteModulesByKb wiring to Story 1.8.
- Dedup after take() in _getChunksForExtraction [convex/knowledge/internal.ts:398-417] — .take(MAX_EMBEDDING_CHUNKS) then .filter() by file_path. For repos with few very large files (many chunks each), unique file coverage may be low. Chunk ordering relies on _creationTime not chunk_index (usually equivalent). Quality concern, not correctness bug.
- Prompt injection via raw code content [convex/knowledge/extractionPrompts.ts] — Source file contents interpolated directly into AI prompts without delimiters or sanitization. Accepted risk for self-service codebase analysis (users analyze own repos). Wrapping in XML fences would harden.
- _deleteModulesByKb unbounded loop [convex/knowledge/internal.ts:362-396] — No max iteration count. Mutation not called in this story (Story 1.8 wires it). EXTRACTION_MAX_MODULES=50 caps practical module count.
- Duplicated error message function [convex/knowledge/extractionActions.ts:37-48] — buildExtractionErrorMessage intentionally differs from buildEmbeddingErrorMessage (extraction-specific messages). Spec said "prefer shared utility" but messages are meaningfully different. Minor duplication.
- Monorepo config files can consume char budget [convex/knowledge/extractionContext.ts:82-122] — Priority sorting puts config files first. 40+ package.json/tsconfig files in a monorepo could consume the 80K char budget before any source code. Edge case quality concern.
- Truncated modules leave dangling dependencies [convex/knowledge/extractionActions.ts:144] — .slice(0, EXTRACTION_MAX_MODULES) can cut modules whose names appear in other modules' dependencies arrays. Rare (50-module repos), minor downstream impact.

## Deferred from: code review of 1-7-module-detail-view (2026-06-13)

- Query errors show infinite loading skeleton [page.tsx:19-25] — `useQuery` error state never inspected. `undefined` (loading) and `null` (not found) handled, but query errors leave user stuck on skeleton. Pre-existing pattern across all pages in the codebase.
- useState(hasItems) stale on data refetch [ModuleSection.tsx:13] — `open` initialized from `hasItems` at mount only. If real-time subscription updates items from empty→populated, section stays collapsed. Minimal practical impact since route navigation causes remount.
- Cross-project module access within same workspace [queries.ts:160-168] — `getModule` checks workspace_id but not project_id. User can access P2's module via P1's route URL. Pre-existing workspace model scope.
- getOptionalMemberWorkspace returns first membership [requireAuth.ts] — Multi-workspace users limited to whichever workspace sorts first. Pre-existing auth pattern not introduced by this story.
- Empty string name renders empty heading [schema.ts] — Schema defines `name: v.string()` with no minimum length. `name: ""` renders empty `<h2>`. Pre-existing schema issue.

## Deferred from: code review of 1-6-knowledge-base-viewer-ui (2026-06-13)

- getKnowledgeBase returns full document while getModules whitelists fields [convex/knowledge/queries.ts:125-148] — Spec mandates returning the raw KB document for bmad_detected forward-compatible access. Future sensitive fields added to knowledge_bases would auto-leak to the client with no review gate. Consider whitelisting the projection when Story 1.9 lands.
- Authorization model inconsistency: getKnowledgeBase uses getOptionalOwnedEntity, getModules uses getOptionalMemberWorkspace [convex/knowledge/queries.ts:106-163] — Spec dev note explicitly prescribed getOptionalMemberWorkspace for getModules. Functionally equivalent in the current single-user workspace model. If multi-user workspaces are ever added, a member could enumerate knowledge_base_ids to read modules — review the auth tier then.
- Duplicated ConvexError message-scraping regex in two files [page.tsx:210, KnowledgeError.tsx:443] — Both run `err.message.replace(/^Uncaught ConvexError:\s*/, "")`. Follows existing project pattern but DRY-violating; the regex depends on Convex's runtime error-wrapping string. Centralize in a helper if a third copy appears.
- seedModule test helper uses unsafe `as Id<>` casts [convex/testHelpers.ts:120-127] — Accepts plain `string` and casts to `Id<"workspaces">` / `Id<"knowledge_bases">`. Test-only, follows existing seed helper pattern. Ideally accept typed Ids directly.
- KnowledgeError stays in error state after successful retry until Convex propagates status [KnowledgeError.tsx] — After triggerIngestion resolves, the button re-enables and error Alert remains until the subscription updates kb.status to "building". Needs optimistic state transition (out of scope for this story).
- getModules unbounded .collect() on large module sets [convex/knowledge/queries.ts:158-163] — Returns entire kb_modules partition. Bounded by architecture detection (~50 modules max). Add pagination if module counts grow.
- kb.status outside {building,ready,error} renders header but blank body [page.tsx:281-303] — Schema enforces the union so this can't occur with valid data. The statusPillMap fallback shows an "Unknown" pill, but no body branch matches. Add a defensive fallback body if the status union ever expands.

## Deferred from: code review of 1-8-knowledge-base-re-sync (2026-06-14)

- TOCTOU race allows concurrent resyncs [convex/knowledge/triggerIngestion.ts:114-147] — `kb_status !== "ready"` check and `_updateKbStatus("building")` are separated by 6 operations, allowing duplicate workflows on concurrent calls. Same pattern as `triggerIngestion`; architectural limitation of Convex actions (no atomic conditional update across query→mutation boundary).
- `deleteAsync` is fire-and-forget — old RAG entries persist briefly during resync [convex/knowledge/embeddingActions.ts] — RAG component's `deleteAsync` schedules background deletion via workpool. Old entries survive until workpool catches up. Search may return stale results in the window between clearRagNamespace returning and background deletion completing. RAG component design limitation.
- `clearRagNamespace` only clears "ready" status entries [convex/knowledge/embeddingActions.ts:192] — RAG `list` defaults to `status: "ready"`, missing "pending" and "replaced" entries from interrupted ingests. Requires RAG API investigation to iterate all statuses.
- KB status stuck at "building" on workflow engine failure [convex/knowledge/triggerIngestion.ts] — if workflow `onComplete` never fires (deployment push, crash), KB is permanently locked. Pre-existing workflow pattern issue affecting all ingestion.
- Auth pattern: `_getProjectForIngestion` not scoped to user's workspace [convex/knowledge/internal.ts:186-201] — `_getMembershipForUser` checks any membership, `_getProjectForIngestion` fetches by ID without workspace linkage. Pre-existing pattern shared with `triggerIngestion`.
- Sequential deletion in `clearRagNamespace` is O(n) round-trips [convex/knowledge/embeddingActions.ts:195-200] — each `rag.deleteAsync` is awaited one at a time. Large namespaces could approach Convex action time limits. Optimization opportunity only.
- `_updateKbStatus` silently clears optional fields when args omitted [convex/knowledge/internal.ts] — Convex `patch` with `undefined` removes fields. Pre-existing API behavior, not a current bug but a landmine for future callers.
- `clearRagNamespace` pagination loop has no max iteration guard [convex/knowledge/embeddingActions.ts:191-203] — extremely large namespaces could exceed Convex action wall-clock limit. Defensive coding concern only.
- Test uses `setTimeout` for creation-time ordering [convex/knowledge.resync.test.ts:72] — relies on 10ms wall-clock gap for distinct `_creationTime` values. Could be flaky on slow CI.
- AI config existence check insufficient [convex/knowledge/embeddingActions.ts:173-175] — checks only `ai_config` truthiness, not `endpoint_url`/`api_key` validity. Pre-existing pattern shared with `embedChunks`.

## Deferred from: code review of 1-9-bmad-artifact-detection-parsing (2026-06-14)

- Delete-then-store not atomic [convex/knowledge/bmadActions.ts:104-112] — Three separate mutations (delete→store→flag). If `_storeBmadMetadata` fails after delete, old metadata is gone and new isn't stored. Mitigated by workflow `retry: true` (idempotent: delete-then-store runs again) and `_setBmadDetected` called last (KB won't show "detected" until full success). Brief inconsistency window acceptable for retry-safe workflow.
- 200 sequential DB inserts in `_storeBmadMetadata` [convex/knowledge/internal.ts:489-498] — Sequential `await` in for-loop for up to 200 entries. Same pattern as `_storeModules`. No parallelism. Performance concern only, not correctness. Convex handles this within transaction limits for reasonable entry sizes.
- Re-sync deletes all data before workflow starts — no rollback [convex/knowledge/triggerIngestion.ts:147-186] — Destructive cleanup (modules, BMAD metadata, chunks, RAG namespace) runs before `start()`. If `start()` throws, KB marked error with all data permanently gone. Pre-existing pattern from Story 1.8, BMAD deletion follows same sequence. No snapshot/archive mechanism.

## Deferred from: code review of 2-1-baseline-rd-generation (2026-06-14)

- **[✅ RESOLVED — post-Epic 3 retro, C5]** No `*-free` model guard — enforced in `getWorkspaceModel` (`convex/ai/model.ts`); all agent factories inherit automatically. Third-epic carry-forward (B4), now enforced codebase-wide.

## Deferred from: code review of 2-2-drift-report-generation (2026-06-14)

- _archiveDriftReport O(n²) read amplification [convex/knowledge/internal.ts:771] — The `.filter((q) => q.neq(q.field("status"), "archived"))` is a post-read database filter applied after the `by_project_id` index lookup. Archived rows are read and filtered out on every iteration. Acceptable at current scale (dozens of reports per project). Would need a compound index including `status` to fix properly. Same pattern as `_archiveBaselineRd` — **baseline's infinite-loop variant (≥100 archived RDs → action timeout → re-sync locked) FIXED in Epic 2 retrospective (2026-06-14) via `.filter((q) => q.neq(q.field("status"), "archived"))` mirroring this function; drift's milder O(n²) read-amplification (archived rows still scanned in index) remains, accepted at current scale.**
- Unsafe type cast on metadata field [convex/knowledge/internal.ts:223] — `e.metadata as { title?: string; status?: string }` without runtime validation. Mirrors `_getBmadMetadataForExtraction:569`. Schema debt from Story 1.5 (`metadata: v.any()` in `kb_bmad_metadata` table).
- Truncation check ignores separator length [convex/knowledge/internal.ts:215-218] — Size check tests `prdSections + chunk` but actual append adds 2-char `\n\n` separator not included in the check. Negligible overshoot (~2 chars per cycle). Copied from `_getBmadMetadataForExtraction`.
- Ingestion auto-trigger tests are source-text inspections [convex/knowledge.driftReport.test.ts] — Tests assert `source.toContain(...)` rather than executing the workflow. The conditional-no-op and failure-isolation behaviors are not behaviorally tested. Same convention as baseline RD workflow tests; full workflow testing needs integration infrastructure not available with convex-test.
- handleRegenerate ignores reason-typed responses [src/app/(auth)/projects/[id]/baseline/drift/page.tsx:44] — `generateDriftReport` returns `{driftReportId: null, reason: "no_old_rd"}` but frontend checks `"error" in result`. Only reachable if Old RD is deleted between `triggerDriftReport`'s guard and the action's internal check (extremely unlikely TOCTOU). Impact: silent no-op, no crash.
- Stale drift report detection not implemented [src/app/(auth)/projects/[id]/baseline/drift/page.tsx] — AC13 requires showing "generating" when report is stale relative to a newer Baseline RD version. Deferred to Story 2.3 which owns the Baseline RD editor and will need staleness tracking anyway.

## Deferred from: code review of 3-2-rag-grounded-responses (2026-06-14)

- AC6 KB-ordering test can't verify latest-KB selection from return shape [convex/knowledge.rag.test.ts:321] — `_getProjectWorkspaceForSearch` returns `{workspace_id, kb_status}` with no KB identity; test verifies DB ordering separately via a raw query. AC6 was pre-satisfied (`.order("desc").first()` already in code). Test-depth only.
- `String.slice` can split UTF-16 surrogate pairs at the truncation boundary [convex/chat/ragContext.ts:14] — RAG text with an emoji/codepoint straddling char 12000 yields a lone surrogate. Rare; a rejecting provider error is caught by the streamText catch. Note `sanitizeTitle` elsewhere uses `Array.from()` for code-point-safe slicing; this module does not.
- KB-ordering test relies on setTimeout(5ms) for `_creationTime` ordering [convex/knowledge.rag.test.ts:330] — flaky on slow CI; recurring pattern (see line 71). `_creationTime` is auto-set and cannot be overridden in seed helpers.
- "no rate token consumed" test asserts only on ragSearchMock, not rate-limiter state [convex/knowledge.rag.test.ts:452-469] — the limit call sits between the KB-status check and `rag.search`, so `ragSearchMock` not firing proves nothing about token consumption. A regression consuming a token on KB-not-ready would pass.
- Pre-existing: `getOptionalMemberWorkspace` uses `.first()` on `by_user_id` [convex/lib/requireAuth.ts:67-71] — multi-membership users resolve to whichever workspace sorts first; `searchProjectRag` inherits this via `_getProjectWorkspaceForSearch` → `getOptionalOwnedEntity`. Already tracked at lines 48, 96; needs cross-cutting `workspace_id`-param fix.
- `buildRagSystemPrompt` doesn't guard against `ragText` containing the literal header `## Retrieved Codebase Context` [convex/chat/ragContext.ts:17] — a retrieved chunk describing this feature would inject a duplicate header, creating prompt-mode ambiguity (the agent keys its grounded/ungrounded behavior on that header's presence). Prompt-injection-adjacent; sanitizing markdown headings from RAG text has no clean fix.

## Deferred from: code review of 3-1-analyst-chat-agent-thread-management (2026-06-14)

- `.unique()` throws unhandled on duplicate `thread_id` [convex/chat/internal.ts:15,61,79] — `verifyThreadOwnership`, `_updateThreadTitle`, and `_updateThreadLastMessageAt` all use `.unique()` on the `by_thread_id` index. If two `chat_threads` rows ever share a `thread_id`, these throw an opaque error. Practically impossible since `thread_id` comes from the Agent Component's UUID generator, and Convex has no unique constraints to enforce it at the schema level. Could harden with `.first()` but risk is negligible.
- `getMemberWorkspace` resolves arbitrary workspace for multi-workspace users [convex/chat/mutations.ts:11, queries.ts:21, queries.ts:47] — `getMemberWorkspace` calls `.first()` on `workspace_members.by_user_id`, returning the membership with earliest `_creationTime`. If a user belongs to multiple workspaces and the target project/thread is in a non-first workspace, the function returns the wrong workspace → "Project not found" / "Thread not found" for a legitimate access. Systemic pre-existing pattern across all domains (knowledge/, workspaces/, etc.), not introduced by this story. Needs a cross-cutting fix: accept `workspace_id` param and look up via `by_workspace_id_and_user_id` index.
- `pnpm build` fails — Task 11 claim false [convex/knowledge/bmadActions.ts, convex/knowledge/baselineActions.ts] — Pre-existing TypeScript circular type inference errors at baseline `d5ff9c0`, NOT caused by Story 3.1 changes. Task 11's `pnpm build` checkbox is marked complete but the build fails. Should be fixed in a separate `fix:` commit and the task annotation corrected.

## Deferred from: code review of 3-3-chat-thread-list-navigation (2026-06-14)

- N+1 component query fan-out in `listThreads` — up to 50 parallel `ctx.runQuery(components.agent.messages.listMessagesByThreadId)` calls per subscription evaluation; re-runs on any `chat_threads` write [convex/chat/queries.ts:68] — Spec Dev Notes explicitly acknowledge & accept this for v1. Performance concern, not correctness. Future optimization: cache `last_message_preview` on the `chat_threads` row (written by `streamMessage` / `_updateThreadLastMessageAt`) and read it directly, eliminating the fan-out.
- `getThread` throws `ConvexError("Not authenticated")` on session expiry while sibling `listThreads` returns `null` — inconsistent graceful-degradation UX between the two chat pages [convex/chat/queries.ts:102] — `getThread` uses `getMemberWorkspace` (throws); `listThreads` uses `getOptionalOwnedEntity` → `getOptionalMemberWorkspace` (returns null). Matches sibling `listThreadMessages` which also throws. Session-expiry query-error UX (line 45: query errors show infinite skeleton) is a codebase-wide deferred concern; not introduced as a regression.
- `getThread` verifies workspace ownership but not that the thread belongs to the URL's `params.id` — `/projects/<wrong-project>/chat/<your-thread>` renders with a misleading "Back to Chat" link to the wrong project [convex/chat/queries.ts:99] — AC8 (workspace ownership / B3 IDOR guard) is satisfied; no cross-workspace data exposed. Project-id binding is defense-in-depth, out of spec scope.
- `truncatePreview` splits base + combining-mark sequences — `Array.from` iterates code points, not grapheme clusters (e.g. `"e\u0301"` = é split at the truncation boundary leaves a dangling base char) [convex/chat/preview.ts:19] — Mirrors the established `sanitizeTitle` pattern from Story 3.1 (same `Array.from` approach). Grapheme-aware segmentation (`Intl.Segmenter`) is a repo-wide truncation-utility enhancement, not story-specific. Minor rendering corruption, no crash.
- Invalid `params.id` (not a valid Convex `Id<"projects">`) → `listThreads` `v.id("projects")` validator rejects before handler runs → `useQuery` stays `undefined` → perpetual skeleton, no error state [src/app/(auth)/projects/[id]/chat/page.tsx:27] — Pre-existing codebase-wide pattern: all `projects/[id]` pages use `asId(params.id)` (a pure type cast, no runtime validation) and hit the same validator-rejection → infinite-skeleton path. Spec explicitly mirrors `knowledge/page.tsx`. Needs a repo-wide client-side ID-validation utility (e.g. `isValidId` guard before the query).

## Deferred from: code review of 4-1-impact-analysis-agent (2026-06-15)

- Multi-workspace users blocked from impact analysis (`getOptionalMemberWorkspace` `.first()`) [convex/chat/internal.ts:21-40 → convex/lib/requireAuth.ts:67-71] — `analyzeImpact` inherits the systemic `.first()` bug: a user with multiple memberships resolves to the oldest workspace, so a thread in a non-primary workspace throws "Thread not found". Inherited from `streamMessage` (same path). Already tracked at lines 99 and 105 above — same cross-cutting fix needed (accept `workspace_id` param, look up via `by_workspace_id_and_user_id`). Not introduced by Story 4.1.

## Deferred from: code review of 3-4-chat-ui-streaming-display (2026-06-15)

- No timeout/abort if `streamMessage` never resolves [src/components/chat/ChatComposer.tsx:51-79] — `handleSubmit` sets `isSending=true` and awaits `streamMessage(...)`. Because `chatActions.ts:113` awaits the full `thread.streamText(...)`, the promise can stay pending for a very long time or hang on a network/agent stall. There is no `AbortController`, no timeout, and no cancel button; the Send button is permanently disabled and the pending optimistic message stays pinned. Only recovery is a full page reload. No AC for cancel/timeout in Story 3.4; Convex `useAction` abort is non-trivial. Out of story scope.
- `inputBase` class string duplicated in `ChatComposer`, not imported from `FormField` [src/components/chat/ChatComposer.tsx:25-26] — The "What NOT to Reinvent" table in the spec said to reuse the existing `inputBase` from `FormField.tsx:22`, but that const is a non-exported local. The composer defines its own `INPUT_BASE` (identical classes + `border-[var(--border)] resize-none min-h-[80px]`). Pragmatic and uses identical classes, but future changes to `FormField.inputBase` will not propagate. Future refactor: `export const inputBase` from `FormField.tsx` and import it here.

## Deferred from: code review of 4-2-user-story-generation (2026-06-15)

- **D1 BMAD section truncation can cut mid-markdown** [convex/chat/storyPrompts.ts:716-722] — Mirrors `impactPrompts.ts:56-61` truncation pattern (`joined.slice(0, EXTRACTION_MAX_CONTEXT_CHARS)` and `RAG_CONTEXT_HEADER` slice can cut mid-bullet / mid-`**bold**`). Model receives malformed markdown. Codebase-wide structural-aware truncation (stop at last `\n\n`) needed across both prompt builders.
- **D2 Stories lost on `_storeUserStories` failure with no UI recovery** [convex/chat/storyActions.ts:1142-1177] — Spec AC11 explicitly accepts the tradeoff ("the thread has the conversation context but the stories aren't in the table — acceptable for drafts; 4.3 owns delete"). The lost-stories-without-UI-recovery concern is real (retry spends LLM tokens for zero user value) but full recovery UI belongs to 4.3 story management.
- **D3 No `by_thread_id` index on `user_stories`** [convex/schema.ts:499-527] — Spec Dev Notes line 415 explicitly states thread-scoped queries are a 4.3 enhancement. `thread_id` is stored on every row but no query uses it yet. Adding the index without a query is premature. When 4.3 builds the thread-history reload, add `.index("by_thread_id", ["thread_id"])` (and ideally `.index("by_thread_id_and_generated_at", ["thread_id", "generated_at"])` for chronological listing).
- **D4 Action not rate-limited at the LLM-call layer** [convex/chat/storyActions.ts] — Spec "What NOT to Reinvent" table inherits the RAG-via-rate-limit pattern from `analyzeImpact` (`ragSearchPerWorkspace` inside `searchProjectRag`). If RAG returns empty, LLM still runs with no rate limit at the action layer. Mirrors `impactActions.ts` — codebase-wide concern. Fix would add a `generatePerWorkspace` rate limiter on both actions.
- **D5 `MAX_FEATURE_REQUEST_LENGTH = 32000` could blow context window combined with system** [convex/chat/storyActions.ts:1008,1144] — Mirrors `impactActions.ts:36-47` exactly. The 32k user prompt + system prompt (STORY_GENERATION_PROMPT + up to `CHAT_RAG_MAX_CONTEXT_CHARS` + up to `EXTRACTION_MAX_CONTEXT_CHARS`) can exceed the model's context window. No token-budget awareness for the user prompt. Codebase-wide refactor needed.
- **D6 `Agent` instance constructed per call** [convex/chat/storyAgent.ts:976-982] — `createStoryGenerationAgent(model)` is invoked inside the action handler, building a fresh Agent each call. Mirrors `impactAgent.ts` factory pattern; established and reviewed-clean in 4.1. If per-instance cost becomes measurable, refactor to a module-level singleton keyed by model.
- **D7 Deep `node_modules` imports in test setup** [convex/chat.stories.test.ts:1264-1275] — `await import("../node_modules/@convex-dev/agent/dist/component/schema.js")` and matching `import.meta.glob` are version-fragile but required by `convex-test`'s module-map pattern. Mirrors `chat.impact.test.ts`. Will silently break on package layout change.
- **D8 `impactResults` and `storyResults` render in fixed order, not chronological** [src/app/(auth)/projects/[id]/chat/[threadId]/page.tsx:226-239] — JSX hard-codes ALL impact cards first, then ALL story cards. Interleaving modes in one thread produces a visual order that doesn't match the conversation flow. Mirrors 4.1's fixed-order pattern. A unified chronological `auxResults: Array<{kind: "impact"|"stories", …}>` array is a cross-feature refactor.
- **D9 Redundant `aria-label` on `<section>` plus `<h4>` inside** [src/components/chat/UserStoriesCard.tsx:1778-1781] — Minor a11y smell: screen readers announce both the section's accessible name and the heading. Mirror of `ImpactAnalysisCard` pattern. Prefer `aria-labelledby` in a future pass.

## Deferred from: code review of 4-3-story-list-status-management (2026-06-15)

- **Cross-project access within same workspace** [convex/stories/queries.ts `getStory`] — `getOptionalOwnedEntity` checks workspace ownership but not project ownership. A story from project P2 renders under P1's URL. Workspace-level is the B3 IDOR security boundary; cross-project-within-workspace is a UX issue. Pre-existing pattern across chat/thread/knowledge queries.
- **Invalid route params cause infinite skeleton** [stories/page.tsx:17, [storyId]/page.tsx:64] — codebase-wide C10 issue. `asId` is a pure type-cast with no runtime validation. Matches chat/page.tsx pattern. Would need a `parseId()` runtime validator returning null on malformed input.
- **Status-filtered query returns wrong top-100 when single status >100 stories** [convex/stories/queries.ts:47-60] — filtered branch takes 100 by `_creationTime` then sorts by `generated_at`. Would need composite index `[project_id, status, generated_at]`. Imausible with forward-only lifecycle but structurally possible with backfilled data.
- **Silent truncation at 100 with no pagination UI** [convex/stories/queries.ts:46,55] — spec Dev Notes explicitly defers pagination to v2.
- **ConfirmDialog lacks role="dialog", focus trap, Escape key** [src/components/ConfirmDialog.tsx] — pre-existing shared component. Full WAI-ARIA dialog pattern needed: `role="dialog"`, `aria-modal="true"`, focus trap, Escape-to-close.
- **Filter state lost when navigating to detail and back** [stories/page.tsx:19] — inherent to useState in App Router. URL search params (`?status=approved`) would persist.

## Deferred from: code review of 4-4-story-export (2026-06-16)

- **Unbounded `ids` array in `getStoriesByIds`** [convex/stories/queries.ts:89] — no `.take()`/length cap. Per explicit AC4 decision ("the selection size is the bound — the BA selected them"). Convex arg-size + query-time limits are the safety net. Defense-in-depth if a future caller/buggy client sends thousands of IDs. Auth-scoped, low practical risk.
- **`getStoriesByIds` filters by `workspace_id` only, not `project_id`** [convex/stories/queries.ts:92] — matches the spec's workspace-level batch-ownership contract and the existing `getStory`/`getOptionalOwnedEntity` pattern (workspace is the B3 IDOR boundary). Today's only caller passes IDs sourced from per-project `listStories`. A future caller passing mixed-project IDs within the same workspace would export them. Documented contract; revisit if a cross-project caller appears.
- **`ExportStories` trigger stuck on "Exporting…" if `getStoriesByIds` errors** [src/app/(auth)/projects/[id]/stories/ExportStories.tsx:81] — the download `useEffect` guards on `stories === undefined`; on query error `useQuery` throws (→ error boundary) rather than returning, so `pendingFormat` could stay non-null if the boundary keeps the subtree mounted. Low-likelihood (session expiry mid-click). Matches the codebase-wide C10 "query errors leave UI pending" pattern.
- **`CopyStoryButton` 2s revert timer not cleared on unmount** [src/app/(auth)/projects/[id]/stories/[storyId]/CopyStoryButton.tsx:34] — `setTimeout(() => setCopied(false), 2000)` has no ref/cleanup. React 18+ no-ops the unmounted setState (no warning); only matters on fast remount where a stale timer could flip fresh `copied` state. Cosmetic.
- **`CopyStoryButton` doesn't guard `navigator.clipboard === undefined`** [src/app/(auth)/projects/[id]/stories/[storyId]/CopyStoryButton.tsx:32] — in a non-secure context the access throws `TypeError` (not a rejection). It is already caught and surfaced via `<Alert>`; only the message quality ("Cannot read properties of undefined...") is suboptimal. App is always served over HTTPS so `navigator.clipboard` is always defined in practice.

## Deferred from: code review of story-5.1 (2026-06-16)

- **Cross-workspace IDOR on `readKnowledgeBase` tool — no auth/ownership check on LLM-supplied `project_id`** [convex/ai/tools/logic.ts:39-48, convex/ai/tools/definitions.ts:42-48] — HIGH. The tool's `execute` trusts the `project_id` the LLM passes (after a format-only `validateConvexId` check); `readKnowledgeBaseLogic` performs no workspace-ownership check. Prompt-injectable content (PRD text, page snapshots) could coerce the model into calling `readKnowledgeBase(project_id="<victim>")` and exfiltrating another workspace's architecture, modules, APIs, data models. **Pre-existing/systemic** — mirrors `readProjectContext`/`readExistingTests`/`readTestCode` (all internal-query tools, trust boundary = agent invocation). The public-surface B3 IDOR hardening belongs on the AI workflow actions (`generatePrdTests`, `generateNlTests`) which resolve project→workspace; this story does not worsen it. Spec Dev Notes "Auth/IDOR Boundary" documents the decision. Fix: resolve authorized `project_id` server-side from the action's workspace context and assert it inside the tool/logic.
- **`validateConvexId` regex lets malformed IDs reach the runtime `v.id()` validator** [convex/ai/tools/definitions.ts:7-9] — MEDIUM. `/^[a-z0-9]{10,}$/i` passes strings like `"abcdefghij"` or a valid-format id from the WRONG table; these then fail the internal query's `v.id("projects")` arg and throw a raw Convex validation error instead of returning the friendly `{ error }` envelope. **Pre-existing/systemic** — identical in all 4 sibling tools; this story's spec mandates exact mirroring of `readProjectContext`. Fix in the shared helper (tighten to a real Convex-id shape, or wrap `ctx.runQuery` in try/catch returning `{ error }`).
- **Unbounded return payload from `readKnowledgeBaseLogic`** [convex/ai/tools/logic.ts:50-67] — MEDIUM. `.collect()` of ALL modules with full `apis`/`data_models`/`user_flows` JSON (schema `v.any()`), no count cap or field-size guard. A large KB risks exceeding Convex query-return / agent tool-result / context-window limits mid-generation. By design — AC2 explicitly specifies returning the FULL grounding data, so bounding would violate the AC. Future hardening: cap module count (`.take(N)`) + truncate/summarize large JSON fields (mirror an `OLD_RD_PREVIEW_LENGTH`-style limit).
- **Tool `execute` / `validateConvexId` error branch untested** [convex/ai/agents.test.ts] — MEDIUM. New tests invoke `readKnowledgeBaseLogic` directly; none exercise `createToolDefinitions().readKnowledgeBase.execute` or the `{ error }` return path. **Pre-existing/systemic test pattern** — AC8 mandates mirroring the `readProjectContextLogic` direct-invocation test, which also skips the execute wrapper; all tool tests share this gap. Improvement: add execute-level tests (success + malformed-id → `{ error }`) across the tool suite.

## Deferred from: code review of story-5.2 (2026-06-16)

- **`.take(10)` truncation can miss a usable RD when a project has >10 consecutive archived/failed versions at the top** [convex/ai/tools/logic.ts:100-106] — MEDIUM. `readBaselineRdLogic` mirrors the two authoritative RD-resolution sites (knowledge/queries.ts:178-187 + knowledge/internal.ts:1019-1034), all using `.take(10)` + `.find(non-archived, non-failed)`. If the 10 highest-version RDs are all archived/failed, an older usable RD beyond position 10 is silently missed → returns `null` despite a usable RD existing. **Pre-existing/systemic** — shared by all three sites; the story spec mandates exact mirroring, so fixing one site alone creates inconsistency. Future hardening: paginate until a usable RD is found or the stream exhausts, OR add a `[project_id, status]` index and query draft/approved directly. Apply across all three sites together.
- **Tool description omits the `{ error }` validation-failure return shape** [convex/ai/tools/definitions.ts:50-61] — MEDIUM. The LLM sees three disjoint return shapes (`ReadBaselineRdResult | null | { error: string }`) with no documented discriminator; the description only mentions the RD object and `null`. An LLM receiving `{ error: "Invalid project_id..." }` may misread it as an empty result instead of a retry-able validation failure. **Pre-existing/systemic** — identical in all 5 sibling tool wrappers; spec mandates mirroring readKnowledgeBase. Align all five: either document the `{ error }` shape in every description, or throw `ConvexError` so the agent framework surfaces it uniformly.
- **Unbounded RD payload returned to the LLM tool** [convex/ai/tools/logic.ts:107-118] — MEDIUM. `sections` is returned verbatim with no section-count cap or per-section content truncation. A draft/approved RD with many large sections could exceed tool-result / context limits mid-generation. Lower risk than the KB case (sections are bounded text, not arbitrary JSON). **Explicitly acknowledged in story-5.2's own Deferred Work section** (parallel to 5.1's unbounded-KB defer). Future hardening: cap total payload size, truncate long section content, mirror the 20K-char convention used by BMAD extraction prompts.
- **No test covers the >10 RDs boundary** [convex/ai/agents.test.ts:555-591] — LOW. The "all archived/all failed → null" tests seed exactly one RD; no test seeds 10+ archived/failed RDs plus an older usable RD, so the `.take(10)` truncation defer above is not regression-protected. Coupled to that defer (testing now would lock in behavior scheduled for change). Add the test when the truncation is addressed.
- **Dev Agent Record test-count claim inaccurate** [5-2 story file: AC8/AC10/Change-Log] — LOW (doc-only). Record claims "18 new tests (9 logic + 8 prompt-builder + 1 wiring)"; actual is 17 (9 + 7 + 1). Coverage still exceeds spec requirements. Trivial doc fix.

## Deferred from: code review of story-5.5 (2026-06-18)

- **[MEDIUM] TOCTOU race between `getStaleTests` and re-sync** [convex/knowledge/queries.ts `getStaleTests`] — Pre-existing race from Story 1.8: `getStaleTests` reads `kb.status` ("ready") and `module_diff` without a lock; a concurrent re-sync can flip status to "building" and clear/replace `module_diff` between reads. P8 adds a `status !== "ready"` early-return that shrinks (but does not eliminate) the window. Full fix requires OCC or a snapshot isolation mechanism (Convex query transactions are read-consistent but not locked across the join fan-out). Spec-deferred; tracked under the same TOCTOU surface as `resyncKnowledgeBase`.
- **[MEDIUM] `internal.ts` exceeds 800-line file-size cap** [convex/knowledge/internal.ts] — Post-review the file is ~880 lines (+80 from the Story 5.5 diff helpers + the P6 deletion net). The `moduleDiff.ts` extraction was the right first cut, but `_snapshotModulesForResync` + `_handleIngestionComplete` could be further extracted into a `convex/knowledge/resync.ts` module. Code-organization concern only; no runtime impact.
- **[LOW] Dedup branch in `getStaleTests` is dead + frontend test over-promises** [convex/knowledge/queries.ts `getStaleTests`; src/app/(auth)/projects/[id]/StaleTestsBanner.test.tsx:87-100] — P3 collapsed the fan-out so each test maps to exactly one suite→exploration→module, making the `Map`-based dedup branch unreachable in normal operation (kept as a defensive guard). The `StaleTestsBanner.test.tsx` "dedup at query level" test feeds a pre-deduped array via the `useQuery` mock, asserting UI behavior the query no longer exercises. Harmless defense-in-depth; the test is slightly over-promised (asserts the component doesn't double-render, not the query's dedup). No action needed unless the dedup branch is removed.
- **[LOW] Unbounded `.collect()` + no render cap in `getStaleTests`** [convex/knowledge/queries.ts `getStaleTests`; src/app/(auth)/projects/[id]/StaleTestsBanner.tsx] — `.collect()` on the workspace's tests partition returns all rows; the banner renders all matches with no `slice(0, N)`. A workspace with thousands of tests across many changed modules would produce a very long banner. Bounded by the practical scale (~50 modules × dozens of suites/tests). Future hardening: `.take(N)` on the test query + a "show more" affordance in the banner.
